{% extends 'navbar.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello bienvenue sur le post aléatoire {{ id }}! ✅</h1>

    <p>Ici vous trouverez toutes les informations concernant le post identifié à l'id {{ id }}, notamment le nombre de d'itération {{ ite }} de la phrase  {{ sentence }}</p>
    

    {% for item in array %}
        <a href="/post/{{ array[item-1] }}">Page numéro {{ array[item-1] }}</a>
    {% endfor %}

    {% for i in 1..ite %}
        <p>{{ sentence }}</p>
        
    {% endfor %}

    <a href="/post/{{ ite }}">Un autre essai ?</a>
</div>
{% endblock %}
